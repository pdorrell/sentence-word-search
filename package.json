{
  "name": "sentence-word-search",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "dev": "vite --open",
    "check-code": "tsc",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:full": "vitest run",
    "build:full": "npm run test:full && npm run build",
    "deploy": "node scripts/update-version.js && npm run build:full && node scripts/deploy-to-target.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@geajs/core": "^1.2.3",
    "compromise": "^14.15.0"
  },
  "devDependencies": {
    "@geajs/vite-plugin": "^1.2.3",
    "jsdom": "^29.0.1",
    "sass": "^1.99.0",
    "typescript": "^6.0.2",
    "vite": "^8.0.3",
    "vitest": "^4.1.2"
  }
}
